# Frontend environment variables (production mode)
# Loaded by Vite when building for production

# Backend API URL — points to the production Cloudflare Worker
VITE_API_URL=https://ember-api.sean-1df.workers.dev

# App name shown in the UI
VITE_APP_NAME=SCCCI CRM
