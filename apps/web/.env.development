# Frontend environment variables (development mode)
# Loaded by Vite when running `vite dev` or building with mode=development

# Backend API URL for local development
VITE_API_URL=http://localhost:8788

# App name shown in the UI
VITE_APP_NAME=Ember (Dev)
